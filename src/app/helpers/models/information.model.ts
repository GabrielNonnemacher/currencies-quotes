import { Currencie } from "./currencie.model";

export interface Information {

}

export class InformationDTO {
    static mapperView = (params: Information, currencie: Currencie): Information => {
        return {

        };
    };
}